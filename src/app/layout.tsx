import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: {
    default: "Hermes Cockpit",
    template: "%s · Hermes Cockpit",
  },
  description:
    "Open-source multi-project mission control for Hermes Agent users. Track autonomous workers, alerts, project health, deployments, and pending work.",
  metadataBase: new URL("https://github.com/goktugozdem2/hermes-cockpit"),
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en" className={`${geistSans.variable} ${geistMono.variable}`}>
      <body>{children}</body>
    </html>
  );
}
